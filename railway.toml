[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "export PATH=$HOME/.nix-profile/bin:$PATH && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"

[service]
rootDirectory = "phase2_research/backend"
